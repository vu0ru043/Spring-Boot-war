pipeline {
    agent any
    stages {
        stage ("Init"){
			steps{
				echo "Start init";
			}   
        }
		stage ("Build"){
            steps{
				echo "Start Build";
			} 
        }
		stage ("Deploy"){
            steps{
				echo "Start Deploy";
			} 
        }
	}
}
